(ns fowl
 (:gen-class)
 (:use [clojure.repl])
 (:require [ontology.functions :refer :all]
           [clojure.string :as str][clojure.java.io :as io][clojure.main :as mn]))

(defn -main [& args]
 
 )
 
